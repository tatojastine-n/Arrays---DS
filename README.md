# Arrays---DS This C# program reads a list of integers from input, reverses the order of the elements using the `reverseArray` function, and then outputs the reversed list.
It uses `List<int>` to store the integers, applies the built-in `.Reverse()` method, and writes the result to the output stream.
